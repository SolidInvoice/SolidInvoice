{#
 # This file is part of SolidInvoice package.
 #
 # (c) Pierre du Plessis <open-source@solidworx.co>
 #
 # This source file is subject to the MIT license that is bundled
 # with this source code in the file LICENSE.
 #
 # Variables:
 #   - plans: list<Plan>
 #   - subscription: Subscription
 #   - currentPlanId: string
 #}

{% extends '@SolidInvoiceCore/Layout/default.html.twig' %}

{% block title %}{{ ux_icon('tabler:arrows-exchange', {width: 24, height: 24, class: 'me-2'}) }}{{ 'saas.change.title'|trans }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-12">
            <div class="d-flex align-items-center justify-content-between mb-4">
                <div>
                    <h1 class="h3 mb-1">{{ 'saas.change.heading'|trans }}</h1>
                    <p class="text-subtle mb-0">
                        {{ 'saas.change.description'|trans }}
                    </p>
                </div>
                <a href="{{ path('billing_index') }}" class="btn btn-outline-secondary">
                    {{ ux_icon('tabler:arrow-left', {width: 18, height: 18}) }}
                    {{ 'saas.action.back'|trans }}
                </a>
            </div>

            {% include '@SolidInvoiceSaas/subscription/_plans_grid.html.twig' with {
                plans: plans,
                currentPlanId: currentPlanId,
                actionRoute: 'saas_subscription_change_confirm',
                csrfToken: 'change_plan',
            } only %}
        </div>
    </div>
{% endblock %}
