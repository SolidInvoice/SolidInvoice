{#
 # This file is part of SolidInvoice package.
 #
 # (c) Pierre du Plessis <open-source@solidworx.co>
 #
 # This source file is subject to the MIT license that is bundled
 # with this source code in the file LICENSE.
 #}

{% extends '@SolidInvoiceCore/Layout/default_no_nav.html.twig' %}

{% block heading %}
    {% include '@SolidInvoiceSaas/subscription/_header.html.twig' %}
{% endblock heading %}

{% block content %}
    {% set availableCompanies = app.user.companies|filter(v => v.id != company_id()) %}

    <div class="subscription-page subscription-cancelled-page">
        <div class="subscription-container">
            {# Hero Icon #}
            <div class="subscription-icon subscription-icon-paused">
                {{ ux_icon('tabler:refresh', {width: 32, height: 32}) }}
            </div>

            {# Headline #}
            <h1 class="subscription-title">{{ 'saas.cancelled.title'|trans }}</h1>
            <p class="subscription-subtitle">
                {{ 'saas.cancelled.subtitle'|trans({'%price%': subscription.plan.price|formatCurrency('USD')}) }}
            </p>

            {# Features Requiring Reactivation #}
            <div class="subscription-benefits">
                <h3>{{ 'saas.cancelled.features_heading'|trans }}</h3>
                <ul>
                    <li>
                        {{ ux_icon('tabler:lock', {width: 20, height: 20}) }}
                        {{ 'saas.cancelled.feature_invoices'|trans }}
                    </li>
                    <li>
                        {{ ux_icon('tabler:lock', {width: 20, height: 20}) }}
                        {{ 'saas.cancelled.feature_payments'|trans }}
                    </li>
                    <li>
                        {{ ux_icon('tabler:lock', {width: 20, height: 20}) }}
                        {{ 'saas.cancelled.feature_history'|trans }}
                    </li>
                    <li>
                        {{ ux_icon('tabler:lock', {width: 20, height: 20}) }}
                        {{ 'saas.cancelled.feature_clients'|trans }}
                    </li>
                    <li>
                        {{ ux_icon('tabler:lock', {width: 20, height: 20}) }}
                        {{ 'saas.cancelled.feature_recurring'|trans }}
                    </li>
                </ul>
            </div>

            {# Primary CTA #}
            <div class="subscription-actions">
                <a href="{{ path('saas_subscription_plans') }}" class="btn btn-primary btn-lg">
                    {{ ux_icon('tabler:arrow-back-up', {width: 20, height: 20}) }} {{ 'saas.cancelled.reactivate'|trans }}
                </a>
                <a href="https://solidinvoice.co/contact" class="btn btn-ghost" target="_blank" rel="external noreferrer noopener">
                    {{ ux_icon('tabler:help', {width: 18, height: 18}) }} {{ 'saas.action.contact_support'|trans }}
                </a>
            </div>

            {# Reassuring Message #}
            <twig:Ui:Alert type="info" class="mt-4 text-start">
                <div class="d-flex align-items-start gap-2">
                    {{ ux_icon('tabler:info-circle', {width: 20, height: 20}) }}
                    <div>
                        <strong>{{ 'saas.cancelled.changed_mind'|trans }}</strong>
                        {{ 'saas.cancelled.data_safe'|trans }}
                    </div>
                </div>
            </twig:Ui:Alert>

            {# Info Alert for Multiple Companies #}
            {% if availableCompanies|length > 0 %}
                <twig:Ui:Alert type="info" class="mt-3 text-start">
                    <div class="d-flex align-items-center gap-2">
                        {{ ux_icon('tabler:building', {width: 20, height: 20}) }}
                        <div>
                            {{ 'saas.company.access_other'|trans }}
                            <a href="{{ path('_switch_company', {'id': availableCompanies|first.id}) }}" class="alert-link">
                                {{ 'saas.company.switch_to'|trans({'%company%': availableCompanies|first}) }}
                            </a>
                        </div>
                    </div>
                </twig:Ui:Alert>
            {% endif %}
        </div>
    </div>
{% endblock content %}
