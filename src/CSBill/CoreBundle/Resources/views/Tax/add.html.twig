{% extends "CSBillCoreBundle:Layout:single_column.html.twig" %}

{% block title %}{{ 'tax.rates.add'|trans }}{% endblock %}

{% block content %}
    <div class="widget">
        {{ form_start(form) }}
        {{ form_row(form) }}
        <button type="submit" class="btn btn-success">{{ icon('save') }} {{ 'tax.rates.save'|trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock content %}
