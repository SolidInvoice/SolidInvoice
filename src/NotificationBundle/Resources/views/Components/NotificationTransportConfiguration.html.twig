{#
# This file is part of SolidInvoice package.
#
# (c) Pierre du Plessis <open-source@solidworx.co>
#
# This source file is subject to the MIT license that is bundled
# with this source code in the file LICENSE.
#}

<div {{ attributes }}>
    <twig:Ui:Card>
        <twig:block name="header">
            <div class="d-flex align-items-center justify-content-between">
                <div>
                    <h3 class="card-title mb-1 d-flex align-items-center gap-2">
                        {{ ux_icon('tabler:bell-ringing', {width: 20, height: 20, class: 'text-primary'}) }}
                        {% if not isNewSetting %}
                            {{ transportSetting.name }}
                        {% else %}
                            {{ 'notification.integration.add'|trans({'%type%': notificationType}) }}
                        {% endif %}
                    </h3>
                    <p class="text-muted small mb-0">
                        {{ 'notification.integration.configure_credentials'|trans }}
                    </p>
                </div>
                <a href="{{ path('_notification_integration') }}"
                   class="btn btn-ghost btn-icon"
                   aria-label="{{ 'action.close'|trans }}">
                    {{ ux_icon('tabler:x', {width: 20, height: 20}) }}
                </a>
            </div>
        </twig:block>

        {{ form_start(form, {
            attr: {
                'data-action': 'live#action:prevent',
                'data-live-action-param': 'save'
            }
        }) }}
            <div class="mb-4">
                {{ form_widget(form) }}
            </div>
        {{ form_end(form) }}

        <twig:block name="footer">
            <div class="d-flex justify-content-between align-items-center gap-3">
                {% if not isNewSetting %}
                    <button type="button"
                            class="btn btn-outline-danger"
                            data-action="live#action"
                            data-live-action-param="showDeleteConfirmation"
                            aria-label="{{ 'notification.integration.delete'|trans }}">
                        {{ ux_icon('tabler:trash', {width: 18, height: 18}) }}
                        {{ 'action.delete'|trans }}
                    </button>
                {% else %}
                    <a href="{{ path('_notification_integration') }}"
                       class="btn btn-ghost">
                        {{ ux_icon('tabler:x', {width: 18, height: 18}) }}
                        {{ 'action.cancel'|trans }}
                    </a>
                {% endif %}
                <button type="submit"
                        class="btn btn-primary btn-save"
                        data-action="live#action"
                        data-live-action-param="save"
                        data-loading="addAttribute(disabled)"
                        aria-label="{{ 'notification.integration.save'|trans }}">
                    {{ ux_icon('tabler:device-floppy', {width: 18, height: 18}) }}
                    {{ 'notification.integration.save_button'|trans }}
                </button>
            </div>
        </twig:block>
    </twig:Ui:Card>

    {# Delete Confirmation Nested Modal #}
    {% if showDeleteConfirmation %}
        <div class="delete-confirmation-backdrop"
             role="presentation"
             aria-hidden="true"
             data-action="click->live#action"
             data-live-action-param="cancelDelete">
        </div>
        <div class="delete-confirmation-modal"
             role="dialog"
             aria-modal="true"
             aria-labelledby="delete-modal-title">
            <div class="delete-confirmation-container">
                <div class="delete-confirmation-header">
                    <div class="d-flex align-items-center gap-3">
                        <div class="delete-icon-wrapper">
                            {{ ux_icon('tabler:alert-triangle', {width: 24, height: 24, class: 'text-danger'}) }}
                        </div>
                        <div>
                            <h4 id="delete-modal-title" class="mb-1">{{ 'notification.integration.delete_title'|trans }}</h4>
                            <p class="text-muted small mb-0">{{ 'notification.integration.delete_undone'|trans }}</p>
                        </div>
                    </div>
                </div>
                <div class="delete-confirmation-body">
                    <p class="mb-0">
                        {{ 'notification.integration.delete_confirm'|trans }} <strong>{{ transportSetting.name }}</strong>?
                        {{ 'notification.integration.delete_warning'|trans }}
                    </p>
                </div>
                <div class="delete-confirmation-footer">
                    <button type="button"
                            class="btn btn-ghost"
                            data-action="live#action"
                            data-live-action-param="cancelDelete">
                        {{ 'action.cancel'|trans }}
                    </button>
                    <button type="button"
                            class="btn btn-danger"
                            data-action="live#action"
                            data-live-action-param="confirmDelete"
                            data-loading="addAttribute(disabled)">
                        {{ ux_icon('tabler:trash', {width: 16, height: 16}) }}
                        {{ 'notification.integration.delete_title'|trans }}
                    </button>
                </div>
            </div>
        </div>
    {% endif %}
</div>
