{#
 # This file is part of SolidInvoice package.
 #
 # (c) Pierre du Plessis <open-source@solidworx.co>
 #
 # This source file is subject to the MIT license that is bundled
 # with this source code in the file LICENSE.
 #}

<div class="py-3">
    {% if client.quotes|length > 0 %}
        <twig:DataGrid name="quote_grid" context="{{ {'client_id': client.id} }}" />
    {% else %}
        <div class="client-empty-state">
            <div class="client-empty-icon">
                {{ ux_icon('tabler:file-text', {width: 32, height: 32}) }}
            </div>
            <p class="client-empty-title">{{ 'client.quotes.empty.title'|trans }}</p>
            <p class="client-empty-description">{{ 'client.quotes.empty.description'|trans }}</p>
            <a href="{{ path('_quotes_create', {'client': client.id}) }}" class="btn btn-primary d-flex align-items-center gap-1 mx-auto" style="width: fit-content;">
                {{ ux_icon('tabler:plus', {width: 16, height: 16}) }}
                {{ 'client.view.actions.create_quote'|trans }}
            </a>
        </div>
    {% endif %}
</div>
