{% extends "@SolidInvoiceCore/Layout/default_no_nav.html.twig" %}

{% block title %}
    Select Company

    {#<form method="post" action="{{ path('_logout_main') }}">#}
    {#<form method="post" action="{{ logout_path() }}">
        <input type="hidden" {{ stimulus_controller('@solidworx/platform/csrf-protection') }} name="_csrf_token" value="{{ csrf_token('logout') }}"/>
        <button type="submit">Logout</button>
    </form>#}
    <twig:Ui:Security:LogoutLink size="sm" color="primary" outline icon="" />
{% endblock title %}

{% block content %}

    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title" id="install-title">
                        {{ 'Choose Company'|trans }}
                    </h3>
                </div>
                <div class="list-group list-group-flush list-group-hoverable">
                    {% for company in companies %}
                        <a href="{{ path('_switch_company', {'id': company.id}) }}" class="list-group-item list-group-item-action">
                            {{ app_logo('50', company) }}
                            {{ company }}
                        </a>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
