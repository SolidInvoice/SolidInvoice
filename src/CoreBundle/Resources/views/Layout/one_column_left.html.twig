{#
 # This file is part of SolidInvoice package.
 #
 # (c) 2013-2015 Pierre du Plessis <info@customscripts.co.za>
 #
 # This source file is subject to the MIT license that is bundled
 # with this source code in the file LICENSE.
 #}

{% extends "SolidInvoiceCoreBundle:Layout:default.html.twig" %}



{% block body %}

    {{ block('content') is defined ? block('content') }}

    {#<aside class="main-sidebar">
        <section class="sidebar">
            <div class="user-panel">
                <div class="pull-left image">
                    #}{#<img src="http://t2.tagstat.com/im/people/silhouette_m_300.png" class="img-circle" alt="User Image">#}{#
                    <img src="https://gravatar.com/avatar/{{ app.user.email|md5 }}?d=mm" class="img-circle" alt="User Image">
                </div>
                <div class="pull-left info">
                    <p>{{ app.user.username }}</p>
                    <a href="{{ path('fos_user_profile_show') }}">{{ icon('envelope') }} {{ app.user.email }}</a>
                </div>
            </div>

            {{ block('sidebar') is defined ? block('sidebar') }}
        </section>
    </aside>

    <section class="content-header">
        {{ block('heading') is defined ? block('heading') }}
    </section>

    <section class="content">

        {% include "SolidInvoiceCoreBundle::flash.html.twig" %}

        {{ block('content') is defined ? block('content') }}
    </section>#}
{% endblock body %}
