{# Photographer preview #}
{% set currency = quote.client.currency %}

<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600;700&display=swap">

<div class="quote-template quote-template-photographer" style="background: #faf3e6; font-family: 'Cormorant Garamond', Georgia, serif; color: #2a2418;">
    <div class="d-flex">
        <div style="background: linear-gradient(180deg, #b85a35 0%, #a2492b 50%, #7d3a22 100%); width: 56px;">
            <div class="text-uppercase text-center py-4" style="color: #faf3e6; letter-spacing: 4px; writing-mode: vertical-rl; font-size: 13px;">
                {{ 'quote.pdf.title'|trans }}
            </div>
        </div>
        <div class="flex-grow-1 p-5">
            {% if setting('system/company/logo') is not empty %}<div class="mb-2">{{ app_logo(48) }}</div>{% endif %}
            <div style="font-size: 42px; line-height: 1;">{{ company_name() }}</div>
            <div class="text-uppercase mb-4" style="color: #7a6a4a; letter-spacing: 2px; font-size: 12px;">{{ 'invoice.template.photographer.subtitle'|trans }}</div>

            <div class="d-flex justify-content-between mb-4">
                <div style="font-size: 28px; color: #a2492b;">{{ 'invoice.number_prefix'|trans }} {{ quote.quoteId }}</div>
                <div class="text-end">
                    <div class="text-muted small text-uppercase">{{ 'quote.pdf.date'|trans }}</div>
                    <div>{{ quote.created|date('F j, Y') }}</div>
                    {% if quote.due %}
                        <div class="text-muted small text-uppercase mt-2">{{ 'quote.pdf.valid_until'|trans }}</div>
                        <div>{{ quote.due|date('F j, Y') }}</div>
                    {% endif %}
                </div>
            </div>

            <div class="row mb-4">
                <div class="col-md-6">
                    <div class="text-uppercase small mb-1" style="color: #a2492b; letter-spacing: 2px;">{{ 'invoice.template.photographer.client_label'|trans }}</div>
                    <div class="fw-semibold fs-5">{{ quote.client }}</div>
                </div>
                <div class="col-md-6">
                    <div class="text-uppercase small mb-1" style="color: #a2492b; letter-spacing: 2px;">{{ 'invoice.template.photographer.studio_label'|trans }}</div>
                    <div class="fw-semibold fs-5">{{ company_name() }}</div>
                </div>
            </div>

            <table class="table" style="background: transparent;">
                <tbody>
                    {% for line in quote.lines %}
                        <tr style="border-bottom: 1px solid #e8d7b8;">
                            <td>
                                <div class="fs-5">{{ line.description|nl2br }}</div>
                                <div class="text-muted small">{{ line.qty }} × {{ line.price|formatCurrency(currency) }}</div>
                            </td>
                            <td class="text-end fs-4 fw-bold" style="color: #a2492b;">{{ line.total|formatCurrency(currency) }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>

            <div class="row justify-content-end mt-4">
                <div class="col-md-5">
                    <div class="d-flex justify-content-between mb-2"><span>{{ 'quote.subtotal'|trans }}</span><span>{{ quote.baseTotal|formatCurrency(currency) }}</span></div>
                    {% if quote.tax.positive %}<div class="d-flex justify-content-between mb-2"><span>{{ 'quote.tax'|trans }}</span><span>{{ quote.tax|formatCurrency(currency) }}</span></div>{% endif %}
                    {% if quote.discount.type is not empty %}<div class="d-flex justify-content-between mb-2"><span>{{ 'quote.discount'|trans }}</span><span style="color: #a2492b;">-{{ discount(quote)|formatCurrency(currency) }}</span></div>{% endif %}
                    <div class="d-flex justify-content-between p-3 rounded" style="background: #a2492b; color: #faf3e6;">
                        <strong>{{ 'quote.total'|trans }}</strong>
                        <strong>{{ quote.total|formatCurrency(currency) }}</strong>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
