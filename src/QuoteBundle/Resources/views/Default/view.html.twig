{#
 # This file is part of SolidInvoice package.
 #
 # (c) 2013-2015 Pierre du Plessis <info@customscripts.co.za>
 #
 # This source file is subject to the MIT license that is bundled
 # with this source code in the file LICENSE.
 #}

{% extends "@SolidInvoiceCore/Layout/one_column_left.html.twig" %}

{% block page_title %}
    {{ "quote.title"|trans({"%id%" : quote.id}) }}
{% endblock page_title %}

{% block sidebar %}
    {{ menu('quotes_view', {'client': quote.client}) }}
{% endblock sidebar %}

{% block content %}

    <div class="pull-right no-print">
        <div class="btn-toolbar">
            <div class="btn-group">
                <a href="{{ path('_quotes_clone', {'id' : quote.id}) }}" class="btn btn-default">
                    {{ icon('clone') }} {{ "quote.view.toolbar.clone"|trans }}
                </a>
            </div>

            <div class="btn-group">
                {% if workflow_can(quote, 'accept') %}
                    <a href="{{ path('_transition_quote', {'action' : 'accept', 'id' : quote.id}) }}" class="btn btn-success">
                        {{ icon('check') }} {{ "quote.view.toolbar.accept"|trans }}
                    </a>
                {% endif %}

                {% if workflow_can(quote, 'decline') %}
                    <a href="{{ path('_transition_quote', {'action': 'decline', 'id' : quote.id}) }}" class="btn btn-danger">
                        {{ icon('times') }} {{ "quote.view.toolbar.decline"|trans }}
                    </a>
                {% endif %}
            </div>
            <div class="btn-group">

                {% if workflow_can(quote, 'reopen') %}
                    <a href="{{ path('_transition_quote', {'action': 'reopen', 'id' : quote.id}) }}" class="btn btn-default">
                        {{ icon('reply') }} {{ "quote.view.toolbar.reopen"|trans }}
                    </a>
                {% endif %}

                <a href="{{ path('_send_quote', {'id' : quote.id}) }}" class="btn btn-info">
                    {{ icon('envelope', ['o']) }} {{ "quote.view.toolbar.send"|trans }}
                </a>
            </div>

            {% if workflow_can(quote, 'cancel') %}
                <div class="btn-group">
                    <a href="{{ path('_transition_quote', {'action': 'cancel', 'id' : quote.id}) }}" class="btn btn-warning">
                        {{ icon('ban') }} {{ "quote.view.toolbar.cancel"|trans }}
                    </a>
                </div>
            {% endif %}

            {% if quote.status != 'accepted' %}
                <div class="btn-group">
                    <a href="{{ path('_quotes_edit', {'id' : quote.id}) }}" class="btn btn-default">
                        {{ icon('edit') }} {{ "quote.view.toolbar.edit"|trans }}
                    </a>
                </div>
            {% endif %}
        </div>
    </div>

    <br class="clear"/>
    <br class="clear"/>

    {% embed "@SolidInvoiceQuote/quote_template.html.twig" %}
        {% block footer %}
            {{ parent() }}

            <div class="row no-print">
                <div class="col-xs-12">
                    <div class="btn-group">
                        <a href="javascript:window.print()" class="btn btn-default">{{ icon('print') }} {{ 'quote.action.print'|trans }}</a>
                        <a href="{{ path('_quotes_view', {'id': quote.id, '_format': 'pdf'}) }}" target="_blank" class="btn btn-default">{{ icon('file-pdf-o') }} {{ 'quote.action.pdf'|trans }}</a>
                    </div>
                </div>
            </div>
        {% endblock footer %}
    {% endembed %}
{% endblock content %}
{% block footer %}
    {{ parent() }}

    {% if quote.notes is not empty %}
        <div class="col-xs-6 no-print">
            <strong>{{ 'quote.notes'|trans }}</strong>
            <p class="text-muted well well-sm no-shadow">
                {{ quote.notes }}
            </p>
        </div>
    {% endif %}
{% endblock footer %}
