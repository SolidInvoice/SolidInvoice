{#
 # This file is part of SolidInvoice package.
 #
 # (c) Pierre du Plessis <open-source@solidworx.co>
 #
 # This source file is subject to the MIT license that is bundled
 # with this source code in the file LICENSE.
 #}

{% autoescape false -%}
    {{ 'reset_password.message_text'|trans({'%username%': user.email, '%confirmationUrl%': url('_user_password_reset', {'token': resetToken.token})}, 'email') }}

    {{ 'This link will expire in %resetTimestamp%.'|trans({'%resetTimestamp%': resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle')}) }}
{%- endautoescape -%}
