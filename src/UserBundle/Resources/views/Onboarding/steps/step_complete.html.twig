{#
# This file is part of SolidInvoice package.
#
# (c) Pierre du Plessis <open-source@solidworx.co>
#
# This source file is subject to the MIT license that is bundled
# with this source code in the file LICENSE.
#}

<div class="onboarding-step-card text-center">
    {# Success Animation #}
    <div class="celebration-icon mb-4">
        <div class="success-checkmark">
            {{ ux_icon('tabler:check', {width: 48, height: 48}) }}
        </div>
    </div>

    <h1 class="h1 mb-3">{{ 'onboarding.complete.title'|trans({}) }}</h1>
    <p class="lead text-muted mb-4">
        {{ 'onboarding.complete.description'|trans({}) }}
    </p>

    {# Next Steps #}
    <div class="step-benefits text-start mb-4">
        <div class="d-flex align-items-start gap-2">
            {{ ux_icon('tabler:checklist', {width: 20, height: 20, class: 'text-primary mt-1'}) }}
            <div>
                <strong class="d-block mb-2">{{ 'onboarding.complete.next_title'|trans({}) }}</strong>
                <ul class="mb-0">
                    {% for step in 'onboarding.complete.next_steps'|trans({})|trim|split("\n") %}
                        <li>{{ step }}</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>

    {# Quick Tips #}
    <div class="onboarding-tip mb-4" role="note">
        {{ ux_icon('tabler:bulb', {width: 20, height: 20}) }}
        <div>
            <strong>{{ 'onboarding.complete.pro_tip_title'|trans({}) }}</strong> {{ 'onboarding.complete.pro_tip_text'|trans({}) }}
        </div>
    </div>

    {% if hasClient %}
        {# Client exists but no invoice yet — direct them to create one #}
        <div class="d-grid gap-3 mb-3">
            <a href="{{ path('_invoices_create') }}" class="btn btn-primary btn-lg">
                {{ 'onboarding.complete.cta_create_invoice'|trans({}) }}
            </a>
        </div>
        {{ form_start(form) }}
            {{ form_widget(form.navigator.finish, {
                'attr': {'class': 'btn btn-link text-muted w-100'}
            }) }}
        {{ form_end(form) }}
    {% else %}
        {# No client or invoice — show next-step mini checklist #}
        <div class="mb-4">
            <p class="fw-semibold mb-2">{{ 'onboarding.complete.next_steps_title'|trans({}) }}</p>
            <div class="list-group text-start">
                <a href="{{ path('_clients_add') }}" class="list-group-item list-group-item-action d-flex align-items-center gap-2">
                    {{ ux_icon('tabler:users', {width: 18, height: 18, class: 'text-primary flex-shrink-0'}) }}
                    {{ 'onboarding.complete.next_add_client'|trans({}) }}
                </a>
                <a href="{{ path('_invoices_create') }}" class="list-group-item list-group-item-action d-flex align-items-center gap-2">
                    {{ ux_icon('tabler:file-invoice', {width: 18, height: 18, class: 'text-primary flex-shrink-0'}) }}
                    {{ 'onboarding.complete.next_send_invoice'|trans({}) }}
                </a>
                <a href="{{ path('_payment_settings_index') }}" class="list-group-item list-group-item-action d-flex align-items-center gap-2">
                    {{ ux_icon('tabler:credit-card', {width: 18, height: 18, class: 'text-primary flex-shrink-0'}) }}
                    {{ 'onboarding.complete.next_connect_payment'|trans({}) }}
                </a>
            </div>
        </div>
        {{ form_start(form) }}
            {{ form_widget(form.navigator.finish, {
                'attr': {'class': 'btn btn-outline-secondary w-100'}
            }) }}
        {{ form_end(form) }}
    {% endif %}

    <div class="mt-4 pt-3 border-top">
        <p class="text-muted small mb-2">
            {{ ux_icon('tabler:heart', {width: 16, height: 16, class: 'text-danger'}) }}
            {{ 'onboarding.complete.thanks'|trans({'%app_name%': app_name}) }}
        </p>
        <p class="text-muted small mb-0">
            {{ 'onboarding.complete.need_help'|trans }}
            <a href="https://solidinvoice.co/docs" target="_blank" rel="noopener" class="text-primary">{{ 'onboarding.complete.docs_link'|trans({}) }}</a>
        </p>
    </div>
</div>
