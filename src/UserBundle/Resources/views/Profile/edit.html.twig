{#
 # This file is part of SolidInvoice package.
 #
 # (c) Pierre du Plessis <open-source@solidworx.co>
 #
 # This source file is subject to the MIT license that is bundled
 # with this source code in the file LICENSE.
 #}

{% set menuItem = 'profile' %}

{% extends "@SolidInvoiceUser/Layout/profile.html.twig" %}

{% block title %}{{ "profile.edit.title"|trans }}{% endblock title %}

{% block profile_content %}
    <div class="profile-form-page">
        {{ form_start(form) }}

        {{ form_errors(form) }}

        <div class="profile-form-container">
            {# Page Header #}
            <div class="profile-form-header">
                <div class="profile-form-header-content">
                    <div class="profile-form-icon">
                        {{ ux_icon('tabler:user-edit', {width: 28, height: 28}) }}
                    </div>
                    <div>
                        <h1 class="profile-form-title">{{ 'profile.edit.title'|trans }}</h1>
                        <p class="profile-form-subtitle">{{ 'Update your personal information and contact details'|trans }}</p>
                    </div>
                </div>
            </div>

            {# Main Form Content #}
            <div class="profile-form-sections">
                <section class="profile-form-section" id="profile-details">
                    <div class="section-header">
                        <div class="section-icon">
                            {{ ux_icon('tabler:settings', {width: 20, height: 20}) }}
                        </div>
                        <div class="section-header-text">
                            <h2 class="section-title">{{ 'Personal Details'|trans }}</h2>
                            <p class="section-description">{{ 'Your name and contact information'|trans }}</p>
                        </div>
                    </div>

                    <div class="section-content">
                        <div class="form-grid">
                            <div class="form-field">
                                {{ form_row(form.firstName) }}
                            </div>

                            <div class="form-field">
                                {{ form_row(form.lastName) }}
                            </div>

                            <div class="form-field form-field-wide">
                                {{ form_row(form.email) }}
                            </div>

                            <div class="form-field form-field-wide">
                                {{ form_row(form.mobile) }}
                            </div>

                            <div class="form-field form-field-wide">
                                {{ form_row(form.current_password) }}
                            </div>
                        </div>
                    </div>
                </section>
            </div>

            {{ form_rest(form) }}

            {# Sticky Footer with Actions #}
            <div class="profile-form-actions">
                <div class="profile-form-actions-inner">
                    <a href="{{ path('_profile') }}" class="btn btn-ghost">
                        {{ ux_icon('tabler:arrow-left', {width: 18, height: 18}) }}
                        {{ 'profile.edit.cancel'|trans }}
                    </a>
                    <button type="submit" class="btn btn-primary btn-save">
                        {{ ux_icon('tabler:device-floppy', {width: 18, height: 18}) }}
                        {{ 'profile.edit.submit'|trans }}
                    </button>
                </div>
            </div>
        </div>

        {{ form_end(form) }}
    </div>
{% endblock profile_content %}
