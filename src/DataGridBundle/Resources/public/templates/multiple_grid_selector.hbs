<div class="grid-selection">
    <div class="float-right">
	<div class="btn-group btn-group-sm" role="group">
	    {{#each grids}}
		<a href="#" class="btn btn-dark{{#if this.disabled}} active disabled{{/if}}" data-grid="{{@key}}">
		    {{#if this.icon}}
			<i class="fas fa-{{this.icon}}"></i>
		    {{/if}}

		    {{this.title}}
		</a>
	    {{/each}}
	</div>
    </div>
    <br class="clear" />
</div>

{{grid.title}}

<div id="{{gridId}}"></div>
