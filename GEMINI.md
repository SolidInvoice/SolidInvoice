# GEMINI.md — SolidInvoice

**Read [`AGENTS.md`](AGENTS.md). It is the source of truth for this repository.**

Everything Gemini needs — commands, house style, testing expectations, migrations,
money/tax rules, translations, PR conventions and the standing no-merge rule — is there,
and only there.

This file used to be a 1658-line copy of `AGENTS.md`. It drifted: it still declared
`**Current Version:** 2.3.11` against a `composer.json` of `3.0.1`. Keeping a copy in sync
by hand does not work, so there is no longer a copy.

If you are about to add a convention here, add it to `AGENTS.md` instead. If `AGENTS.md`
is wrong, fix `AGENTS.md`.
